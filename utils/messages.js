const LINK_NOT_PROVIDED = 'Must provide the full link as an argument'
const INVALID_URL = 'is not a valid URL'
const LINK_ALREADY_EXISTS = 'A link with this code already exists. It points to'

module.exports = {
  LINK_NOT_PROVIDED,
  INVALID_URL,
  LINK_ALREADY_EXISTS,
}
